{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for Dev Tray (tray-first; window optional)",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
